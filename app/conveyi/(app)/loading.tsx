/** The shell stays; the page area says so while the next page arrives. */
export default function Loading() {
  return <div style={{ color: '#94a3b8', fontSize: 13, padding: '24px 0' }}>Loading…</div>;
}
